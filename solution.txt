📋 STAGE-BY-STAGE COMMANDS
STAGE 1: Basic Reversal
bash
./linked_lab
# Output: Stage 1 intro

ltrace ./linked_lab
# Hidden message: FLAG{basic_reversal_complete}

# Program run :
./linked_lab
# Input: FLAG{basic_reversal_complete}
# → STAGE 2 UNLOCK!

========================================================
STAGE 2: Password Extraction
bash
ltrace -e strcmp ./linked_lab
# Input: anything

# Output में दिखेगा: strcmp_password_found

./linked_lab
# Input: strcmp_password_found
# → STAGE 3 UNLOCK!

========================================================
STAGE 3: Multiple Credentials
bash
ltrace -e strcmp -s 256 ./linked_lab
# Input: anything
#        anything

# Output में सब usernames और passwords दिखेंगे

./linked_lab
# Input: admin_master (या कोई और valid username)
#        MasterPassword@321 (या कोई और valid password)
# → STAGE 4 UNLOCK!

========================================================
STAGE 4: Long Password
bash
ltrace -e strcmp -s 512 ./linked_lab
# Input: anything

# VERY LONG password दिखेगा

./linked_lab
# Input: पूरा long password
# → STAGE 5 UNLOCK!

========================================================
STAGE 5: Function Frequency
bash
ltrace -c ./linked_lab
# Output में strlen count दिखेगा

./linked_lab
# Input: strlen कितनी बार call हुई (7)
# → STAGE 6 UNLOCK!

========================================================
STAGE 6: Time Analysis
bash
ltrace -t ./linked_lab
# Input: anything

# Timestamps के साथ auth code दिखेगा

./linked_lab
# Input: TIME_BASED_AUTH_CODE
# → STAGE 7 UNLOCK!

========================================================
STAGE 7: Child Process
bash
ltrace -f ./linked_lab
# Input: CHILD_PROCESS_MASTER_KEY

./linked_lab
# फिर से run करो
# Input: CHILD_PROCESS_MASTER_KEY
# → STAGE 8 UNLOCK!

========================================================
STAGE 8: PID Monitoring
bash
# Terminal 1:
./linked_lab
# Note the PID (e.g., 12345)

# Terminal 2:
ltrace -p 12345 -e strcmp

# Terminal 1 में:
# Input: PID_MONITORING_SUCCESS
# → STAGE 9 UNLOCK!

========================================================
STAGE 9: Library Tracking
bash
ltrace -l libc.so.6 ./linked_lab
# Input: anything

# libc functions में से key निकालो: LIBC_MASTERY_COMPLETE

./linked_lab
# Input: LIBC_MASTERY_COMPLETE
# → STAGE 10 UNLOCK!

========================================================
STAGE 10: ULTIMATE FINAL
bash
# सब stages के knowledge को combine करो
# Final key: FLAG{LTRACE_ULTIMATE_MASTER_2024}

./linked_lab
# Input: FLAG{LTRACE_ULTIMATE_MASTER_2024}
# → LAB COMPLETE! 🏆
